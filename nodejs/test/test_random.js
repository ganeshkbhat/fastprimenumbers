
var expect = require('chai').expect;
const primes = require("../index");

describe('test_random.js:: [Test A] Test Suite for random functions', function () {

    it('[Test A] tests for all prime random ', function (done) {
        expect(primes.random).to.equal();
        done();
    });

});
